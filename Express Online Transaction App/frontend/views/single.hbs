{{>header}}
{{>navbar}}
  {{#if isFound}}

  <section class="row">
    <table class="table table-stripped">
      <thead>
        <tr class="bg-green table-header">
          <th>Account Number: {{userData.accNum}} </th>
          <th>Name: {{userData.name}} </th>
          <th>Initial Balance: {{userData.initBalnce}} </th>
          <th>Remaining Balance: {{userData.remainingBalance}} </th>

        </tr>

      
      </thead>


    </table>

    <table>
      <thead>      
           <tr >        
            <th colspan="3"  >Operations </th>
</tr>
</thead>

        {{#each allOperations}}
        <tr colspan="4">
           <td>{{operation}}</td>
          <td>{{value}}</td>
          <td>
        </tr>
         {{/each}}
    </table>
  </section>
    {{else}}
  <div class="alert alert-danger">
    User Not Found
  </div>
  {{/if}}
</div>
{{>footer}}